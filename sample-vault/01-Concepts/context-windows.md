# Why context window size matters for RAG

A context window defines how much text a language model can process at once.
In a RAG system, retrieved documents must fit in the window alongside the user query.
Larger windows allow more context but increase cost and latency.
