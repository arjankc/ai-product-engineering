# Travel Log

Use one entry per day: date, places visited, actual spend, transport used, accessibility observations, and one lesson for the next day.

Never rewrite a planned activity as completed. Mark entries as `planned`, `confirmed`, `completed`, or `cancelled` so later summaries preserve provenance.
