# Cosine similarity for embeddings

Embeddings are lists of numbers that represent meaning.
Cosine similarity measures how close two embeddings are: 1.0 is identical direction, 0 is unrelated.
RAG ranks vault chunks by cosine similarity to the query embedding.
