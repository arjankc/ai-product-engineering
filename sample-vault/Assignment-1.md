# Assignment 1 — JavaScript warm-up

**Due:** Friday of Week 3, 23:59 local time  
**Language:** JavaScript (Node 20+). No TypeScript for this assignment.

## Requirements

- Implement `normalizeQuery(raw)` that trims and truncates to 500 characters.
- Include at least three unit-style assertions in a `test.mjs` file (plain `assert` is fine).
- Do **not** use external npm packages beyond what the starter already lists.

## Rubric highlights

- Correctness of edge cases (`null`, empty string, long strings): 40%
- Clear function naming and comments: 20%
- Tests that fail when the function is broken: 40%

## Forbidden

- Submitting minified one-liners with no tests.
- Copying solutions from prior cohorts without citation (honor code violation).
